{{!
  The page that closes a successful request. It reports that the one action
  happened and nothing else: the secret itself is never rendered.
}}<html><body>{{#unlocked}}Unlocked. Credential delivered to local worker.{{/unlocked}}{{^unlocked}}Stored.{{/unlocked}}</body></html>
